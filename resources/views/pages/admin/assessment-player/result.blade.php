@extends('layouts.app')
@section('content')
<x-common.page-breadcrumb pageTitle="Assessment Result" />
<div class="mx-auto max-w-3xl space-y-6"><div class="rounded-2xl border p-8 text-center {{ $attempt->passed ? 'border-success-200 bg-success-50 dark:border-success-500/30 dark:bg-success-500/10' : 'border-error-200 bg-error-50 dark:border-error-500/30 dark:bg-error-500/10' }}"><p class="text-sm font-medium uppercase {{ $attempt->passed ? 'text-success-600' : 'text-error-600' }}">{{ $attempt->passed ? 'Passed' : 'Not passed' }}</p><h1 class="mt-2 text-5xl font-bold text-gray-900 dark:text-white">{{ $attempt->score_percentage }}%</h1><p class="mt-2 text-gray-600 dark:text-gray-300">{{ $attempt->earned_marks }} of {{ $attempt->total_marks }} marks · Attempt {{ $attempt->attempt_number }}</p></div>
<x-common.component-card :title="$attempt->assessment->title" desc="Answer review"><div class="space-y-4">@foreach($attempt->assessment->questions as $question)@php($answer = $attempt->answers->firstWhere('assessment_question_id', $question->id))<article class="rounded-xl border border-gray-200 p-4 dark:border-gray-800"><div class="flex justify-between gap-3"><h3 class="font-medium text-gray-800 dark:text-white">{{ $loop->iteration }}. {{ $question->prompt }}</h3><x-ui.badge :color="$answer?->is_correct ? 'success' : 'error'">{{ $answer?->is_correct ? 'Correct' : 'Incorrect' }}</x-ui.badge></div><p class="mt-2 text-sm text-gray-500">Earned {{ $answer?->earned_marks ?? 0 }} / {{ $question->marks }} marks</p></article>@endforeach</div><div class="mt-5 flex justify-end gap-3"><a href="{{ route(\App\Support\PortalRoute::name('results.index')) }}" class="rounded-lg border border-gray-300 px-4 py-2.5 text-sm dark:border-gray-700 dark:text-white">All results</a>@can('assessments.take')<a href="{{ route('learning.assessments.index') }}" class="rounded-lg bg-brand-500 px-4 py-2.5 text-sm text-white">Back to tests</a>@endcan</div></x-common.component-card></div>
@endsection
