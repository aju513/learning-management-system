@extends('layouts.app')
@section('content')
<x-common.page-breadcrumb pageTitle="My Tests" />
<div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($assessments as $assessment)<article class="rounded-2xl border border-gray-200 bg-white p-6 dark:border-gray-800 dark:bg-white/[0.03]"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-medium uppercase text-brand-500">Standalone quiz</p><h2 class="mt-1 text-lg font-semibold text-gray-800 dark:text-white">{{ $assessment->title }}</h2></div><x-ui.badge color="success">Available</x-ui.badge></div><p class="mt-3 text-sm text-gray-500">{{ Str::limit($assessment->description, 120) }}</p><dl class="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt class="text-gray-500">Questions</dt><dd class="font-medium text-gray-800 dark:text-white">{{ $assessment->questions_count }}</dd></div><div><dt class="text-gray-500">Duration</dt><dd class="font-medium text-gray-800 dark:text-white">{{ $assessment->duration_minutes }} min</dd></div><div><dt class="text-gray-500">Pass score</dt><dd class="font-medium text-gray-800 dark:text-white">{{ $assessment->passing_percentage }}%</dd></div><div><dt class="text-gray-500">Attempts</dt><dd class="font-medium text-gray-800 dark:text-white">{{ $assessment->attempts_count }}/{{ $assessment->max_attempts }}</dd></div></dl><form method="POST" action="{{ route('learning.assessments.start', $assessment) }}" class="mt-5">@csrf <button class="w-full rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-medium text-white disabled:opacity-50" @disabled($assessment->attempts_count >= $assessment->max_attempts)>Start / continue quiz</button></form></article>@empty<div class="col-span-full rounded-2xl border border-dashed border-gray-300 p-12 text-center dark:border-gray-700"><h2 class="font-semibold text-gray-800 dark:text-white">No standalone quizzes available</h2><p class="mt-1 text-sm text-gray-500">Quizzes assigned directly to you will appear here.</p></div>@endforelse</div>
@endsection
