@extends('layouts.app')
@section('content')
<x-common.page-breadcrumb pageTitle="Assessment Results" />
<x-common.component-card title="Attempt history" desc="Scores and pass/fail outcomes are immutable after grading.">
    <form method="GET" class="grid gap-3 sm:grid-cols-[1fr_180px_auto]"><x-form.select name="assessment_id" :options="$assessments" :value="request('assessment_id')" placeholder="All assessments" /><x-form.select name="passed" :options="['1' => 'Passed', '0' => 'Failed']" :value="request('passed')" placeholder="All outcomes" /><button class="h-11 rounded-lg border border-gray-300 px-4 text-sm dark:border-gray-700 dark:text-white">Filter</button></form>
    <div class="overflow-x-auto"><table class="min-w-full divide-y divide-gray-200 dark:divide-gray-800"><thead><tr class="text-left text-xs uppercase text-gray-500"><th class="px-2 py-3">S.N.</th><th class="px-4 py-3">Trainee</th><th class="px-4 py-3">Assessment</th><th class="px-4 py-3">Attempt</th><th class="px-4 py-3">Score</th><th class="px-4 py-3">Outcome</th><th class="px-4 py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($results as $result)<tr><td class="px-2 py-4 text-sm text-gray-500">{{ $results->firstItem() + $loop->index }}</td><td class="px-4 py-4"><p class="font-medium text-gray-800 dark:text-white">{{ $result->trainee->name }}</p><p class="text-xs text-gray-500">{{ $result->trainee->email }}</p></td><td class="px-4 py-4 text-sm text-gray-700 dark:text-gray-300">{{ $result->assessment->title }}</td><td class="px-4 py-4 text-sm text-gray-600 dark:text-gray-300">#{{ $result->attempt_number }}</td><td class="px-4 py-4 font-semibold text-gray-800 dark:text-white">{{ $result->score_percentage }}%</td><td class="px-4 py-4"><x-ui.badge :color="$result->passed ? 'success' : 'error'">{{ $result->passed ? 'Pass' : 'Fail' }}</x-ui.badge></td><td class="px-4 py-4 text-right"><a href="{{ route(\App\Support\PortalRoute::name('results.show'), $result) }}" class="rounded-lg border border-gray-300 px-3 py-2 text-xs text-brand-500 dark:border-gray-700">View</a></td></tr>@empty<tr><td colspan="7" class="px-4 py-10 text-center text-sm text-gray-500">No graded attempts found.</td></tr>@endforelse</tbody></table></div>{{ $results->links() }}
</x-common.component-card>
@endsection
